# Rime default settings
# encoding: utf-8

config_version: "0.16.wasm"

schema_list:
  - schema: luna_pinyin

switcher:
  hotkeys:
  save_options:
    - full_shape
    - ascii_punct
  fold_options: true
  abbreviate_options: true

menu:
  page_size: 100

punctuator:
  full_shape:


key_binder:
  bindings:

recognizer:
  patterns:

ascii_composer:
  switch_key:
